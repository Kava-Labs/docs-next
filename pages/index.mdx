import { RenderMe } from '../components/example.jsx'; 
import { Layout } from '../components/Layout.jsx'; 

# MDX + Next.js

Look, a button! 👇


<RenderMe />

export default ({ children }) => <Layout>{children}</Layout>